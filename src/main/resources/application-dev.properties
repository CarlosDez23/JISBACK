# Development profile (Railway + Netlify)
spring.jpa.show-sql=false

# CORS - Netlify dev + local dev servers
cors.allowed-origins=https://jis-test.netlify.app,http://localhost:4200,http://localhost:3000
